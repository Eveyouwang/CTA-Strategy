# BLOCKED

阻塞项：无。

## 已自行判断、请复核
1. **数据来源**：任务书界限写「data/ 只读、不许联网下载（没有 token）」，但本机没有 data/；任务书末尾追加了 tushare token 与代理地址并要求用 TuShare 取数，按追加内容一次性下载后只读。数据能否随仓库公开到 GitHub，要看 tushare 的用户协议；不能公开就删掉 data/，别人用 `src/fetch_data.py` 和自己的 token 重下（同一接口数据可能随 tushare 修订而变，sha256 会不同）。token 出现在对话里，没有写进任何文件。
2. **L 最小变动价位**：合约表对全部 L 合约写 1 元/吨，日线显示 2021-11-01 前为 5 元/吨，滑点按后者计（证据见 `python -m src.check_data` 末尾与 `tests/test_data.py::test_l_tick_follows_prices`）。
3. **第④步方向性平仓**：任务书只列了①②③。网格要求测平仓阈值 0，原规则 |z|<0 永不成立，所以加了这一步，并单独记指标变化。
4. **成交量口径**：按 tushare 原始数据，未核实两个交易所历年单边、双边统计口径，报告里成交量只作量级参考。
5. **手续费**：按任务书用名义额万分之一，未查实际费率；成本敏感性另测 0、2 跳滑点。
6. **③窗口规则事后改过**：预定「半衰期取整」得 290 天，在持组自有历史不够，零交易；改成「不超过半衰期的最大网格窗口」120 天。改的时候已看过样本内网格（没看样本外），请判断是否接受。
7. **选中参数在网格角上**（窗口 120 为最大、开仓 2.5 为最大、平仓 0 为最小），邻域只有 8 格，趋势提示最优可能在网格外（更长窗口、更高开仓阈值）；按任务书没有扩网格。
8. **换月规则缺陷（样本外运行后发现，未改）**：规则取「可选组里前一交易日 MA 持仓量最大」。2022 年起郑商所甲醇 06、10、11、02 等月份持仓量一度超过下一个 01/05/09 合约，样本内有 5 次在截止日先换到这些月份、随后又换到 01/05/09，多付换月成本、DCE 腿也落在非主力月。可改为只在 01/05/09 中选或要求新组持仓量领先若干天，但这属于样本外之后改规则，需要领导决定是否另起一轮（新规则只能在新的样本外上检验）。
