# PROGRESS

断点续跑：先读本文件「进度」一节，从第一个未勾选项接着做，已勾选的不重做。

## 目标／顺序／最大风险（任务 0 完成后、动工前写）
1. 目标：用 tushare 日线如实复现 tqsdk 示例 mto_spread.py，定量缺陷、逐项修正、样本内稳健性、冻结参数后样本外只跑一次；报告所有数字由 run_all.py 生成
2. 顺序：数据体检 → 换月日历与窗口（含红绿反向验证）→ 原版 → 逐项修正 → 网格选参、单独提交 params.json → 样本外一次 → 报告
3. 风险一：换月跳空伪造信号。对策：每组合约只用自己的历史算 μ、σ，测试覆盖换月日
4. 风险二：样本外间接参与调参。对策：任务 2–4 只用 2025-06-30 及以前；params.json 的提交早于样本外结果
5. 风险三：日线复现与原脚本逐 tick 触发不同；早期流动性差；L 跳价规则中途变过。对策：报告写明，样本起点按流动性定，滑点按当时跳价

## 数据来源
- 任务书写的 data/ 两个文件本机不存在（主目录下全盘搜索无结果）。用户在任务书末尾给了 tushare token 与代理地址，要求读 TuShare 文档取数。按此用 `src/fetch_data.py` 于 2026-09-11 一次性下载，token 走环境变量不入库；下载后 `chmod a-w` 只读
- fut_basic(fut_type=1)：三品种全部普通合约 546 个；fut_daily：最后交易日 ≥ 2014-01-01 的 471 个合约逐合约日线，共 109,487 行；耗时 1515 秒，代理 SSL 断连重试 6 次
- sha256（run_all.py 每次运行先核对）：
  - `daily_MA_L_PP.csv`  5ed7d15fc3d3562c1eb798efeac4621b90838935bd12f2155347c6647eaf8341
  - `contracts_MA_L_PP.csv`  ac2452795c1953f682f8103963a99c3a15d9d296ddcb6290b1f31a4c80b0b8df

## 任务 0 实测（`python -m src.check_data`）
- python 3.12.7；pandas 2.2.2、numpy 1.26.4、matplotlib 3.9.2、statsmodels 0.14.2、pytest 7.4.4
- 合约数（合约表／有日线）：L 239／164，PP 160／160，MA 147／147；日线首日 L 2013-01-18、PP 2014-02-28、MA 2014-06-17，末日均为 2026-09-11
- 乘数：multiplier 列三品种全空；每手吨数在 per_unit 列，MA 10、L 5、PP 5，与预期一致，不触发停工
- 郑商所代码：ts_code 用 4 位年月（MA2409.ZCE），symbol 用 3 位（MA409），任务书里「猜的」这一条已核实
- 两个交易所 2014-06-17 起交易日完全一致；合约存续期内无缺行；无重复行；无非正价格
- 缺失：成交量为 0 的行 L 13,956、PP 12,356、MA 7,087；这些行开高低为空，大商所收盘等于结算价，郑商所收盘为空；结算价无缺失
- L 最小变动价位：合约表写 1 元/吨；有成交行开高低收全是 5 的整数倍的比例，2021-11-01 前按年都在 99.6% 以上，2021-11-01 当天起降到 5% 以下；PP、MA 始终约一成 → L 此前为 5 元/吨
- 早期流动性：换月日历 2014-06 至 2015-04 在持 1506 组（当时 10 吨/手的 MA 只有远月），其 L、PP 腿 2014 年日成交中位数仅 24 手、2 手，2014-12 至 2015-04 有 30 天某腿无成交；2015-05 起在持组三腿每天都有成交

## 决策记录（为什么这么做）
- 项目目录 `~/mto-spread-review`：工作目录是用户主目录，不能在主目录 git init
- 取数用标准库 urllib 按 tushare SDK 同样的协议发请求：依赖白名单不含 tushare；代理拒绝 urllib 默认 UA（实测 403），改用 requests 的 UA
- 每手吨数取 per_unit：tushare 的 multiplier 列对商品期货为空
- L 滑点按当时跳价：2021-11-01 前 5 元/吨，之后 1 元/吨（数据推断，已写 BLOCKED.md 请复核）；常量 `L_TICK5_BEFORE`，有测试
- 无成交日补价：收盘缺失用结算价，开盘缺失用补后的收盘价（期货按结算价盯市）；主要影响原版持有到期那几天和远月历史
- 样本内起点 2015-05-04：按上面的流动性数据定，定的时候没跑过任何回测
- 原版推广到多年：从起点当天换月日历所指的组开始，一组合约用到最后交易日收盘强平，次日换下一组（顺序同换月日历），相当于合约到期后手工改代码；另单独复现原脚本写死的 2409 合约与 2023-11-01 至 2024-04-30
- 换月日历：前一交易日 MA 持仓量最大的组，只向后换；「交割月前一个月首个交易日」前 3 个交易日起不再选用，保证收盘、次日开盘两种口径都在交割月前一个月之前换完；换月当天同方向移仓，平旧开新都计成本
- 止损阈值 = 1.5 × 开仓阈值：原脚本写法 `STD_THRESHOLD * 1.5`
- 冷静期：止损后 |z| 回到开仓阈值以内才允许再开
- ③窗口：取样本内去换月跳空价差的 AR(1) 半衰期四舍五入，规则先定再跑回测
- 加第④步方向性平仓（持空 z<exit、持多 z>-exit 即平）：网格要测平仓阈值 0，原规则 |z|<0 永不成立；原规则在 z 越过均值后不平仓本身也是缺陷
- 网格窗口 30 指过去 30 个交易日（原版 29 = 30 根 K 线去掉当根）
- params.json 放 `report/params.json`：可改动白名单里没有根目录文件
- 选参：保守口径（次日开盘、1 跳）夏普，3×3×3 邻域平均最大者
- ③窗口改动（看过样本内网格之后改的，如实记录）：预定规则「半衰期取整」得 290 天；每组合约在持时自有历史只有一百多天，290 天窗口下样本内 z 一天也算不出，v3、v4 零交易。改为「不超过半衰期的最大网格窗口」= 120 天。改的时候已看到网格里 120 天各格夏普偏高，报告里写明
- 测试写错一处：`test_directional_exit` 期望日期错一位（z_path[p] 对应 dates[p+1]），改的是测试期望，引擎没动

## 进度
- [x] 任务 0 数据体检
- [x] 任务 1：pytest 14 passed；反向验证 A（窗口含当日 S.shift(0)）test_data 2 failed → 还原 5 passed；B（最后可用日改到截止日后 25 天）1 failed → 还原 5 passed；首次提交 63e19da
- [x] 任务 2（样本内，保守口径 1 跳）：原版多年推广 年化 5.0%、夏普 0.07、最大回撤 112%、158 笔、胜率 53.8%；乐观口径夏普 -0.02；2409 demo 区间 8 笔、夏普 1.66
- [x] 任务 3：夏普（保守）v0 0.07 → v1 冷静期 0.07 → v2 换月 0.15 → v3/v4 见报告（窗口 120）
- [x] 任务 4：去跳空连续价差 ADF p=0.27、半衰期 290 天；逐组 31 组仅 3 组 p<0.05。网格保守口径夏普 -0.49~0.37。邻域规则选 窗口120/开仓2.5/平仓0（本格 0.27、邻域 0.18）；单点最高 60/2.0/0（0.37、邻域 0.04）。选中格在网格角上，报告写明
- [ ] 任务 4 收尾：params.json 单独提交
- [ ] 任务 5 样本外（只跑一次）
- [ ] 任务 6 报告
